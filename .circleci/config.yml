version: 2
jobs:
   build:
     machine: true
     steps:
       - checkout
       - run: ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no $DROPLET_USER@$DROPLET_IP;
       - run: cd /var/www/html
       - run: git pull
       - run: npm install
       - run: npm run build